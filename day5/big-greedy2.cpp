#include <string>
#include <vector>

using namespace std;

int find_biggest_in_scope(string num){
    // 주어진 숫자 서브스트링 내에서 가장 큰 숫자를 찾아 그 인덱스를 반환
    int idx = 0;
    int biggest = num.front();
    for(int i=0; i<num.size(); i++){
        num.at(i)
    }


}

string solution(string number, int k) {

    int cnt = 0;
    for(int i=0; i<number.size(); i++){
        if(cnt == k) break; // k개의 숫자 다 뺀 경우 종료
        number.at(i);
    }


    string answer = "";
    return answer;
}